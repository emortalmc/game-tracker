apiVersion: apps/v1
kind: Deployment

metadata:
  name: game-tracker
  namespace: emortalmc
  labels:
    app: game-tracker

spec:
  replicas: 3
  selector:
    matchLabels:
      app: game-tracker

  template:
    metadata:
      annotations:
        linkerd.io/inject: enabled
      labels:
        app: game-tracker
    spec:
      containers:
        - name: game-tracker
          image: ghcr.io/emortalmc/game-tracker:f2f1c3f4ff406ba3c1bc0873c59a515115cc77a9
          ports:
            - name: grpc
              containerPort: 9010

          env:
            - name: "DEVELOPMENT"
              value: "false"
            - name: KAFKA_HOST
              value: default-kafka-bootstrap
            - name: KAFKA_PORT
              value: "9092"
            - name: MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: game-tracker-db-creds
                  key: connection-string
            - name: PORT
              value: "9010"